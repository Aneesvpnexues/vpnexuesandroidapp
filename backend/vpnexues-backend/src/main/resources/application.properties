spring.application.name=vpnexues-backend

# H2 in-memory database (default - for quick start)
spring.datasource.url=jdbc:h2:mem:vpnexues_db
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
spring.jpa.defer-datasource-initialization=true

# Server port (Render sets PORT env var)
server.port=${PORT:8080}

# CORS
app.cors.allowed-origins=*

# Gmail SMTP
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=sachinvpnexues@gmail.com
spring.mail.password=pwkucdlxvaptwaam
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
